apiVersion: v1
kind: Pod
metadata:
  name: web5
  labels:
    state: dev
spec:
  containers:
  - name: myapp
    image: <Image>
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: <Port>
