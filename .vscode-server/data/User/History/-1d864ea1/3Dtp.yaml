apiVersion: v1
kind: Pod
metadata:
  name: web3
spec:
  containers:
  - name: web3
    image: <Image>
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: <Port>
